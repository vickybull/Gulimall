spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://175.24.0.121:3306/gulimall_ums?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.jdbc.Driver

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml  # *表示扫描包括引用其他的jar包中的相应的路劲
  global-config:
    db-config:
      id-type: auto   # 设置id的自增长
server:
  port: 8082
